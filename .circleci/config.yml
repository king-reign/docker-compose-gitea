version: 2.1

jobs:
  Build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Build Docker"
          command: "docker build . -t mygitea"
workflows:
  kickoff:
    jobs:
      - Build
